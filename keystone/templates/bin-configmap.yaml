apiVersion: v1
kind: ConfigMap
metadata:
  name: keystone-bin
  labels:
    system: openstack
    type: configuration
    component: keystone

data:
  common.sh: |
{{ include "common.sh" .| indent 4 }}
  keystone-bootstrap: |
{{ include "keystone/templates/bin/_keystone-bootstrap.tpl" . | indent 4 }}
  keystone-seed: |
{{ include "keystone/templates/bin/_keystone-seed.tpl" . | indent 4 }}
  keystone-start: |
{{ include "keystone/templates/bin/_keystone-start.tpl" . | indent 4 }}
