apiVersion: v1
kind: ConfigMap
metadata:
  name: ironic-bin
  labels:
    system: openstack
    type: configuration
    component: ironic

data:
{{ (.Files.Glob "bin/*").AsConfig | indent 2 }}
  common.sh: |
{{ include "common.sh" . | indent 4 }}
